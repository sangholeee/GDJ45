목록 가져와서 보여주기 - 모델
    1. forward : request에 목록을 저장해서 보여줘야 하므로
    2. list.jsp으로 전달하기

삽입하기 - 모델
    1. redirect : forward는 요청이 2번 발생하므로 삽입이 2번 시도되므로 redirect만 가능
    2. main.jsp으로 이동하기

-----------------------------------------------------------------------------------------

모델별로
    1. forward / redirect 결정하기
    2. 이동할 jsp 결정하기

-----------------------------------------------------------------------------------------

   class ActionForward {
   
      private boolean isRedirect;    // redirect이면 true, forward이면 false
      private String view;           // 이동할 jsp 이름

   }

-----------------------------------------------------------------------------------------











