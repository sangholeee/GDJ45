<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.goodee.ex11.mapper.EmployeeMapper">

	<!-- 전체 레코드 갯수 -->
	<select id="selectEmployeeCount" resultType="Integer">
		SELECT COUNT(*)
		 FROM EMPLOYEES
	</select>
	
	<!-- BEGIN~END 사이 목록 반환 -->
	<!-- resultType이 List 일때는 domain으로 사용한다. -->
	<select id="selectEmployees" parameterType="Map" resultType="Employee">
		SELECT A.RK, A.EMPLOYEE_ID, A.FIRST_NAME, A.LAST_NAME, A.EMAIL, A.PHONE_NUMBER, A.HIRE_DATE, A.JOB_ID, A.SALARY, A.COMMISSION_PCT, A.MANAGER_ID, A.DEPARTMENT_ID
  		  FROM (SELECT RANK() OVER(ORDER BY HIRE_DATE DESC) AS RK, EMPLOYEE_ID, FIRST_NAME, LAST_NAME, EMAIL, PHONE_NUMBER, HIRE_DATE, JOB_ID, SALARY, COMMISSION_PCT, MANAGER_ID, DEPARTMENT_ID
  		  		  FROM EMPLOYEES) A
 		 WHERE A.RK BETWEEN #{beginRecord} AND #{endRecord}
	</select>
	
</mapper>